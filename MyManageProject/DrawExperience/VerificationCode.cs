using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DrawExperience
{
    public class VerificationCode
    {
        //public uint[] RandomNums(uint codeNum = 5)
        //{
        //    codeNum = codeNum > 20 ? 20 : codeNum;
        //    uint[] randoms = new uint[codeNum];
        //    Random rand = new Random();
        //    for (int i = 0; i < codeNum; codeNum++) {
               
        //    }
        //}
    }
}
